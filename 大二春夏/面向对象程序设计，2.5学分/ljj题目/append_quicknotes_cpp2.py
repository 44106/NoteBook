from pathlib import Path


entries = [
    ("2-1", "拷贝构造常用于按值传参时复制对象。", "按值传参会创建副本；不是“传给另一个类”。"),
    ("2-2", "`obj2(10,20)` 调用两个 `int` 参数的构造函数。", "构造函数匹配看参数个数和类型。"),
    ("2-3", "构造函数名必须与类名相同。", "析构函数前面有 `~`，不是构造函数。"),
    ("2-4", "对象按引用传参不会生成副本，因此不会多出一个局部副本析构。", "引用不是新对象。"),
    ("2-5", "对象通常有属性、状态、行为，名字不是对象本质属性。", "名字只是程序员给的标识。"),
    ("2-6", "静态成员函数中直接使用非静态成员会编译报错。", "静态成员函数没有 `this`。"),
    ("2-7", "友元函数是非成员函数，但可访问类的私有成员。", "它不是成员函数。"),
    ("2-8", "题库把封装和抽象的区别记为“封装是绑定，抽象是隐藏”。", "这是一种教材化说法，不够严格。"),
    ("2-9", "私有成员函数只能通过类的公有接口间接使用。", "类外不能直接调私有成员函数。"),
    ("2-10", "访问私有数据应提供公有成员函数。", "不要直接把数据成员改成 public。"),
    ("2-11", "涉及动态内存的类通常应自定义拷贝构造。", "否则可能发生浅拷贝问题。"),
    ("2-12", "标准拷贝构造语法是 `ClassName(const ClassName&)`。", "少了引用会造成递归拷贝问题。"),
    ("2-13", "拷贝构造参数应按引用传递。", "按值传递会再次调用拷贝构造。"),
    ("2-14", "拷贝构造若不按引用接收，可能导致无限复制甚至内存问题。", "核心原因是递归调用自身。"),
    ("2-15", "编译器在生成临时对象时会调用拷贝构造。", "按值返回、按值传参都常见。"),
    ("2-16", "题库认为动态内存类需要用户自定义拷贝构造。", "本质是深拷贝需求。"),
    ("2-17", "拷贝构造函数可以声明为 private。", "常用于禁止拷贝。"),
    ("2-18", "拷贝构造参数通常应为 `const` 引用。", "否则无法接收 const 对象和临时对象。"),
    ("2-19", "构造函数重载指同一类中多个不同签名的构造函数。", "返回类型不能区分重载。"),
    ("2-20", "构造函数没有返回类型，因此不能靠返回类型重载。", "构造函数连 `void` 都不写。"),
    ("2-21", "析构函数既可隐式调用，也可显式调用。", "实际开发中通常依赖隐式调用。"),
    ("2-22", "正常情况下构造了多少对象，就会析构多少对象。", "异常退出或资源泄漏题面通常不考虑。"),
    ("2-23", "题库认为析构函数应为 public。", "否则类外对象离开作用域时可能无法正常析构。"),
    ("2-24", "析构函数不能重载。", "一个类只能有一个析构函数。"),
    ("2-25", "析构函数用于释放对象占用的资源。", "不是“删除类”本身。"),
    ("2-26", "析构函数在对象生命周期结束前自动调用。", "不是程序运行中任意时刻。"),
    ("2-27", "全局对象在程序结束前析构。", "顺序通常与构造相反。"),
    ("2-28", "析构函数与构造函数最明显区别是前者有 `~`。", "构造函数没有返回类型，析构函数也没有。"),
    ("2-29", "全局对象析构顺序通常与构造顺序相反。", "记住“后构造先析构”。"),
    ("2-30", "类中若管理动态资源，通常应自定义析构函数。", "否则可能泄漏内存。"),
    ("2-31", "public 类可通过对象实例在其他位置访问。", "题面用了 Java 风格说法，按题库记。"),
    ("2-32", "类没有 public 成员时，类外不能调用其成员函数。", "不是不能创建对象。"),
    ("2-33", "类中的变量和常量统称数据成员。", "member variables 常对应 data members。"),
    ("2-34", "数据成员可在声明处或构造函数中初始化。", "题库按现代/混合写法处理。"),
    ("2-35", "静态数据成员通常在类外定义，而不是在构造函数中定义。", "构造函数可修改值，但不是“定义”。"),
    ("2-36", "类中使用结构体类型成员前，结构体必须先定义。", "编译器要先知道类型。"),
    ("2-37", "想在类外拿到私有成员地址，通常通过公有成员函数返回。", "类外不能直接取私有成员地址。"),
    ("2-38", "代码输出 `n50100`。", "成员 `n=100`，`m=50`，输出时前面还有字母 `n`。"),
    ("2-39", "静态成员函数只能直接使用静态数据成员。", "非静态成员需要对象。"),
    ("2-40", "题库把可继承但对子类保持私有语义的成员对应到 `protected`。", "这题表述很差，按答案记。"),
    ("2-41", "局部类的成员函数默认会被当作内联处理。", "局部类只在其定义函数内部可见。"),
    ("2-42", "不同函数中的局部类可以同名。", "因为它们处于不同局部作用域。"),
    ("2-43", "局部类的作用域仅限其所在函数。", "类外和其他函数都不可见。"),
    ("2-44", "除包围它的函数外，其他函数不能访问局部类成员。", "局部类作用域很窄。"),
    ("2-45", "局部类能为特定函数增加局部辅助功能。", "它不是提速工具。"),
    ("2-46", "嵌套类就是定义在类内部的类。", "不是定义在函数里。"),
    ("2-47", "题库认为继承减少了嵌套类的使用。", "本质是通过继承复用结构，而非层层嵌套。"),
    ("2-48", "非静态嵌套类可访问外围类的所有成员。", "题库按这种说法记，但标准语义更细。"),
    ("2-49", "静态嵌套类不能直接访问外围类的其他成员。", "尤其不能直接访问非静态上下文。"),
    ("2-50", "嵌套类可提升代码组织性和可读性。", "不是让代码自动并发。"),
    ("2-51", "对象在类外通常只能访问 public 成员。", "private/protected 不能直接访问。"),
    ("2-52", "对象不是用来定义成员函数的。", "成员函数属于类定义。"),
    ("2-53", "标准输入对象是 `cin`。", "对应标准输出对象 `cout`。"),
    ("2-54", "一个对象同一时刻只能是一个类类型的实例。", "不会同时属于多个无关类。"),
    ("2-55", "不同类里使用同名对象名在同一作用域会编译报错。", "同一作用域内名字不能重复定义。"),
    ("2-56", "题库认为派生类友元可访问基类的非私有成员。", "友元关系不继承，这题要按答案记。"),
    ("2-57", "要扩展功能，最直接做法是再加一个公有乘法函数。", "没必要暴露内部数据。"),
    ("2-58", "类是面向对象程序设计的核心载体。", "不是过程式特征。"),
    ("2-59", "类适合把相近的数据和操作组织在一起。", "这正是封装。"),
    ("2-60", "访问器方法用于让私有数据可控地被外部访问。", "不是把私有数据直接变 public。"),
    ("2-61", "常成员函数不会修改调用对象的状态。", "通常声明为函数后缀 `const`。"),
    ("2-62", "私有成员函数只能在类内部使用。", "类外对象不能直接调用。"),
    ("2-63", "类的公有成员函数可以访问该类的私有成员。", "这是封装的基础。"),
    ("2-64", "公有成员函数可通过对象在类外访问。", "是否能访问看对象和作用域。"),
    ("2-65", "题库认为按继承方式保持同样变化的是 public 成员函数。", "实际要结合继承方式整体理解。"),
    ("2-66", "不能直接在 `main` 里像普通函数一样调用某对象的公有成员函数名。", "需要对象或类作用域。"),
    ("2-67", "类的公有成员函数在类内部可访问该类所有成员。", "包括 private/protected。"),
    ("2-68", "静态数据成员是类所有对象共享的一份数据。", "不是每个对象各有一份。"),
    ("2-69", "修改静态数据成员会影响该类所有对象看到的结果。", "因为它是共享的。"),
    ("2-70", "静态数据成员声明语法是 `static type name;`。", "顺序不能颠倒。"),
    ("2-71", "静态数据成员通常还需在类外定义一次。", "只在类内声明不算完整定义。"),
    ("2-72", "静态数据成员定义格式是 `type Class::member = value;`。", "中间必须是 `::`。"),
    ("2-73", "题库认为访问静态数据成员的成员函数应是静态成员函数。", "实际普通成员函数也能访问静态成员。"),
    ("2-74", "静态数据成员可以通过对象用点运算符访问。", "更常见也更推荐用类名访问。"),
    ("2-75", "通过类名访问静态数据成员要写 `ClassName::member`。", "不是 `.` 或 `->`。"),
    ("2-76", "示例静态成员输出结果是 `21 22`。", "共享状态会在连续调用中累积。"),
    ("2-77", "静态数据成员在整个类中只保留一份。", "不是每个对象一份副本。"),
    ("2-78", "静态成员函数可通过点或箭头运算符调用。", "也可用类名直接调用。"),
    ("2-79", "题库代码里最先输出的是 `Result` 的构造信息。", "依代码定义和静态对象初始化顺序而定。"),
    ("2-80", "题库给的静态成员函数调用语法并不标准，但按答案记该项。", "真正标准写法是 `ClassName::functionName()`。"),
    ("2-81", "题库把静态成员函数理解成供所有对象共享的一份成员函数。", "实际普通成员函数也只有一份代码。"),
    ("2-82", "静态成员函数只能直接访问类的静态成员。", "不能直接访问非静态成员。"),
    ("2-83", "静态成员函数可以直接用类名调用。", "无需对象。"),
    ("2-84", "题库给的类名调用语法没有括号，按答案记该项。", "真正调用应写 `ClassName::functionName()`。"),
    ("2-85", "`this` 是指向当前对象的指针。", "只存在于非静态成员函数中。"),
    ("2-86", "静态成员函数不能是虚函数。", "虚函数依赖对象动态绑定和 `this`。"),
    ("2-87", "静态成员在程序中只创建并初始化一次。", "不是每建一个对象创建一次。"),
    ("2-88", "静态成员函数可以重载。", "重载看参数列表，不看是否 static。"),
    ("2-89", "题库记忆点：`this` 不能声明为 const/volatile/const volatile。", "这里是老题式表述，标准语义更细。"),
    ("2-90", "静态关键字在类内声明时写，类外定义时一般不再重复写。", "题库答案是这个记忆点。"),
    ("2-91", "`this` 保存当前对象的实际地址。", "因此可区分不同对象。"),
    ("2-92", "若对象本身不传递，可把需要的数据成员分别传递。", "题库强调“拆开传”。"),
    ("2-93", "把字符串赋给不匹配的对象引用会编译报错。", "引用类型必须兼容。"),
    ("2-94", "按值传参会分配新的对象存储并复制内容。", "按引用不会产生新对象。"),
    ("2-95", "对象既可按值返回，也可按引用返回。", "要注意引用返回的生命周期。"),
    ("2-96", "对象按值返回通常会产生临时对象。", "现代编译器可优化，但题库按传统语义考。"),
    ("2-97", "题库把 `ClassName functionName(){}` 视为对象返回函数的正确写法。", "函数返回类型是类名。"),
    ("2-98", "按引用返回对象的函数写法是 `ClassName& functionName()`。", "返回引用要保证目标对象仍然有效。"),
    ("2-99", "局部对象通常不能按引用安全返回。", "函数结束后局部对象已销毁。"),
    ("2-100", "错误返回局部对象引用可能导致运行时错误。", "典型是悬空引用。"),
    ("2-101", "按引用返回对象时不需要额外创建临时对象。", "因为直接引用已有对象。"),
    ("2-102", "对象可以作为函数返回值。", "按值返回是允许的。"),
    ("2-103", "题库认为两个对象可指向同一内存位置。", "更准确说是对象内部指针可指向同一地址。"),
    ("2-104", "对象按值传参时，其成员值通过拷贝构造复制。", "不是简单按地址共享。"),
    ("2-105", "按引用返回不创建临时对象，因为只是返回已有对象的别名。", "题库原句有语法问题，但意思如此。"),
    ("2-106", "对象赋值的本质是把一个对象的值复制给另一个对象。", "前提是同类或可赋值类型。"),
    ("2-107", "题库认为对象直接复制到对象。", "本质对应同类对象赋值或拷贝。"),
    ("2-108", "函数实参在按值传递时会借助拷贝构造初始化。", "不是简单裸内存拷贝。"),
    ("2-109", "题库代码输出 `10`。", "通常考浅拷贝或成员独立性。"),
    ("2-110", "若未定义拷贝赋值运算符，编译器会隐式生成一个。", "默认是逐成员赋值。"),
    ("2-111", "若拷贝构造只接收非常量引用，则只能拷贝非常量对象。", "不能接收 const 对象和临时对象。"),
    ("2-112", "赋值号在声明初始化和普通赋值里的语义场景不同。", "一个是初始化，一个是赋值。"),
    ("2-113", "静态成员从任一对象修改后，对所有对象可见。", "共享数据的典型特征。"),
    ("2-114", "用已有对象在声明时初始化新对象属于拷贝初始化。", "不是普通赋值。"),
    ("2-115", "可把一个引用变量所引用的值赋给另一个引用相关对象。", "引用一旦绑定，不能改绑对象。"),
    ("2-116", "按引用传递对象不会再创建副本。", "效率更高。"),
    ("2-117", "同类对象之间可以直接赋值。", "编译器通常提供默认赋值操作。"),
    ("2-118", "访问对象成员最直接用对象名。", "配合点运算符。"),
    ("2-119", "`this` 指针大小与普通指针大小相同。", "本质上它就是一个指针。"),
    ("2-120", "对象指针调用成员函数用 `pointer->function()`。", "不是 `pointer.function()`。"),
    ("2-121", "释放后未置空的指针会变成悬空指针。", "再使用会有未定义行为。"),
    ("2-122", "题库把 `*` 视为取出指针所指内容的操作。", "严格说地址本身通常直接保存在指针变量里。"),
    ("2-123", "若不希望通过指针修改所指内容，应把所指对象声明为 const。", "题库简写成“指针 const”，但更准确是 pointee const。"),
    ("2-124", "当前对象地址由 `this` 指针保存。", "只在非静态成员函数中可用。"),
    ("2-125", "`this` 会在成员函数调用时隐式传入。", "程序员通常不显式写出它。"),
    ("2-126", "`this` 只存在于非静态成员函数中。", "静态成员函数没有 `this`。"),
    ("2-127", "`this` 不属于对象本身存储的一部分。", "它是调用时隐式传入的指针。"),
    ("2-128", "`sizeof(object)` 不包含 `this` 指针本身的额外空间。", "对象大小只看成员和对齐。"),
    ("2-129", "调用成员函数时，对象地址会作为隐式参数传入。", "这正是 `this` 的来源。"),
    ("2-130", "题库把成员函数调用抽象为 `function(&object, parameter)`。", "这是理解编译器转换的一种近似。"),
    ("2-131", "`this` 可在成员函数内部直接使用。", "类外不能直接写 `this`。"),
    ("2-132", "`this` 常用于防止自赋值。", "典型写法是 `if (this == &other)`。"),
    ("2-133", "题库认为 `if(&object != this)` 是可执行判断式。", "其他选项语法不合法。"),
    ("2-134", "题库记忆点：`this` 不可被修改。", "现代标准下 `this` 本身不可重新赋值。"),
    ("2-135", "早期实现中曾允许对 `this` 赋值。", "现代 C++ 不允许这么做。"),
    ("2-136", "`this` 可用于操作自引用数据结构。", "比如链表、树等对象内部链接。"),
    ("2-137", "题库把 `this` 形式记为 `[cv] classType *const this`。", "重点是它本质是常指针。"),
    ("2-138", "构造函数可通过不同参数列表重载。", "不能靠返回类型区分。"),
    ("2-139", "若希望既可无参又可带参构造，可用全默认参数构造函数。", "这样一个构造函数覆盖两种场景。"),
    ("2-140", "构造函数名必须和类名完全相同。", "否则就只是普通函数。"),
    ("2-141", "所有参数都有默认值的构造函数可充当默认构造函数。", "可无参调用。"),
    ("2-142", "默认参数参与重载时，容易让多个构造函数调用产生歧义。", "重载设计要避免冲突。"),
    ("2-143", "同一类里不能有多个“全默认参数构造函数”。", "否则无参调用会歧义。"),
    ("2-144", "调用 `className(5,'a')` 会匹配 `className(int x=5, char c='a')`。", "参数类型和顺序都匹配。"),
    ("2-145", "`className(int x=0, char c)` 会编译报错。", "默认参数后面不能跟无默认参数。"),
    ("2-146", "全默认参数构造函数在不传参时会使用全部默认值。", "这正是它代替默认构造的原因。"),
    ("2-147", "题库把零参数构造函数称为默认构造函数。", "更广义地说，可无参调用的也算。"),
    ("2-148", "与其写一个无参和一个单参默认值构造，不如直接写一个默认值构造。", "可减少歧义。"),
    ("2-149", "`new` 用于分配内存并返回相应指针。", "通常还会调用构造函数。"),
    ("2-150", "`new` 失败时，题库记忆点是“可能抛异常，也可能返回 0”。", "现代默认 `new` 抛 `bad_alloc`，`nothrow new` 才返回空指针。"),
    ("2-151", "`new` 先分配内存，再调用构造函数。", "分配和构造是两个步骤。"),
    ("2-152", "题库给出的二维数组 `new` 写法按答案记。", "这题写法本身并不标准可靠。"),
    ("2-153", "`new` 不能分配引用类型。", "引用不是可独立分配的对象。"),
    ("2-154", "`new` 不能直接分配函数，但可分配函数指针。", "函数本身不是普通对象。"),
    ("2-155", "用 `new` 分配的对象不会因离开作用域自动销毁。", "必须手动 `delete` 或交给智能指针。"),
    ("2-156", "题库把 `new T` 视为隐式调用 `::operator new(sizeof(T))`。", "这是理解底层机制的经典表述。"),
    ("2-157", "`delete` 用于释放动态分配的内存块。", "并在需要时先调用析构函数。"),
    ("2-158", "`delete` 没有返回值。", "不是返回 0 或指针。"),
    ("2-159", "`delete` 表达式结果类型按题库记为 `void`。", "重点是不能拿它当有意义值使用。"),
    ("2-160", "释放错误的内存或重复释放会导致不可预测错误。", "典型未定义行为。"),
    ("2-161", "`delete` 可以安全作用于空指针。", "对空指针执行 `delete` 没问题。"),
    ("2-162", "`delete` 时先调用析构，再释放内存。", "析构顺序早于 deallocation。"),
    ("2-163", "指针被释放后其值变成未定义状态。", "应主动置为 `nullptr`。"),
    ("2-164", "释放动态数组要用 `delete[] objectName;`。", "不能用普通 `delete`。"),
    ("2-165", "`delete` 最终会调用 `operator delete`。", "这是其底层释放机制。"),
    ("2-166", "题库认为 `delete` 会调用全局 `operator delete`。", "除非类重载了专用版本。"),
    ("2-167", "题库把内建类理解成语言预定义类。", "更像“库中已有类”的旧式说法。"),
    ("2-168", "预定义类或库类使用前通常需要先包含相应头文件。", "否则编译器不知道声明。"),
    ("2-169", "题库认为内建类不包含对象。", "这题质量一般，按结论记。"),
    ("2-170", "处理标准输入流的对象是 `cin`。", "与输入流概念关联。"),
    ("2-171", "对象数组是一组同类对象，用一个数组名统一表示。", "数组元素类型必须一致。"),
    ("2-172", "对象数组中的所有元素必须属于同一类。", "不能混放不同类对象。"),
    ("2-173", "类是对象数组元素所属的类型定义。", "对象数组本质上是类实例集合。"),
    ("2-174", "对象就是类的实例。", "类是模板，对象是实体。"),
    ("2-175", "对象可以作为函数参数传递。", "可按值、引用或指针。"),
    ("2-176", "题库代码输出 `24`。", "通常考静态成员或对象共享行为。"),
    ("2-177", "局部对象只能在当前函数内声明、访问和使用。", "离开作用域后销毁。"),
    ("2-178", "函数内局部对象不能被其他函数直接使用。", "除非以参数/返回值形式传递。"),
    ("2-179", "对象按值返回时会创建临时对象。", "现代编译器可做返回值优化。"),
    ("2-180", "同类对象之间可直接赋值，如 `s1 = s2;`。", "不同类对象默认不能直接赋值。"),
]


def main() -> None:
    path = Path("c++练习题目二：类和对象.md")
    content = path.read_text(encoding="utf-8")
    if "## 中文速记版" in content:
        return
    lines = ["", "## 中文速记版", ""]
    for qid, conclusion, warning in entries:
        lines.append(f"- **{qid}**：结论：{conclusion} 易错点：{warning}")
    path.write_text(content.rstrip() + "\n" + "\n".join(lines) + "\n", encoding="utf-8")


if __name__ == "__main__":
    main()
